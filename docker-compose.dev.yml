version: '3.8'

services:
  voice-bot:
    build: 
      context: .
      dockerfile: Dockerfile.dev
    container_name: voice-bot-dev
    ports:
      - "8081:8000"
    volumes:
      - .:/app
    env_file:
      - .env
